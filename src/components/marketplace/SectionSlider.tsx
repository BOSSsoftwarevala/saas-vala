import React from 'react';

// Dummy SectionSlider component to prevent build errors
export const SectionSlider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <div className="section-slider">{children}</div>;
};
